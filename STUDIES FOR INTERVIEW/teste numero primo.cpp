#include <bits/stdc++.h>
using namespace std;

// O(raiz n)

boolean isPrime(int n) {
	for (int x = 2; x <= sqrt(n); x++) {
		if (n % X == 0) {
			return false;
		}
	}
	return true;
}

int main(){
	setlocale(LC_ALL,"");
	
	return 0;
}

